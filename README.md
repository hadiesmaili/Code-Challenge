# 📝 Task Manager

**Tasks** یک اپلیکیشن مدیریت وظایف (Task Management) با کارایی بالا و طراحی مدرن است که با تمرکز بر تجربه کاربری (UX) و اصول مهندسی نرم‌افزار توسعه یافته است.

## ۱. قابلیت‌های اصلی (What it does)

- **مدیریت وظایف:** ایجاد، حذف و تغییر وضعیت کارها.
- **تغییر اولویت بصری:** قابلیت کشیدن و رها کردن (Drag & Drop) برای مرتب‌سازی لیست.
- **فیلترینگ هوشمند:** دسته‌بندی کارها بر اساس وضعیت (همه، انجام شده، در جریان).
- **چندزبانه (i18n):** زیرساخت آماده برای پشتیبانی از زبان‌های مختلف.
- **طراحی مدرن:** رابط کاربری Glassmorphism و Responsive با استفاده از Tailwind CSS.
- **پایداری وضعیت:** مدیریت State یکپارچه با Redux Toolkit.

## ۲. تکنولوژی‌های به‌کار رفته (Tech Stack)

- **React 19** (Functional Components & Hooks)
- **Redux Toolkit** (State Management)
- **@dnd-kit** (Drag and Drop primitives)
- **Tailwind CSS** (Styling)
- **i18next** (Internationalization)
- **Lucide React** (Icons)
- **TypeScript** (Type Safety)

## ۳. راهنمای نصب و اجرا (Installation)

### پیش‌نیازها

قبل از شروع، مطمئن شوید ابزارهای زیر روی سیستم شما نصب هستند:

1. **Git:** برای مدیریت نسخه‌ها ([دانلود Git](https://git-scm.com/))
2. **Node.js:** نسخه LTS پیشنهاد می‌شود ([دانلود Node.js](https://nodejs.org/))

### مراحل اجرا

۱. ابتدا پروژه را کلون کنید:

```bash
git clone [URL_PROJECT]
cd [PROJECT_FOLDER]
```

۲. وابستگی‌های پروژه را نصب کنید:

```bash
pnpm install
```

۳. پروژه را در حالت توسعه اجرا کنید:

```bash
pnpm dev
```

برنامه در آدرس `http://localhost:3000` در دسترس خواهد بود.

## ۴. ساختار پوشه‌بندی (Folder Structure)

پروژه بر اساس ساختار **Feature-based Modular Architecture** سازماندهی شده است:

- `src/components/ui`: کامپوننت‌های پایه و Reusable (مثل Button).
- `src/features/tasks`: شامل کامپوننت‌های اختصاصی مربوط به ماژول Task.
- `src/store`: پیکربندی Redux و Sliceهای پروژه.
- `src/hooks`: هوک‌های اختصاصی برای جداسازی منطق بیزینسی از UI.
- `src/i18n`: فایل‌های تنظیمات ترجمه و زبان.
- `src/lib`: توابع کمکی و Utilityها.

## ۵. نکات مهم برای توسعه (Development Guidelines)

- **Atomic Design:** کامپوننت‌های کوچک و مستقل بسازید.
- **Strict Typing:** تمامی Interfaceها و Typeها باید در فایل `types.ts` تعریف شوند.
- **Business Logic:** منطق‌های پیچیده را به جای کامپوننت، در `hooks/useTasks.ts` بنویسید.
- **Performance:** از `React.memo` و `useCallback` برای بهینه‌سازی رندرینگ در لیست‌های بزرگ استفاده کنید.
- **Conventional Commits:** برای ثبت تغییرات در Git، از استانداردهای نام‌گذاری (مثل `feat:`, `fix:`, `refactor:`) استفاده کنید.

---

توسعه یافته با ❤️ برای شما.
